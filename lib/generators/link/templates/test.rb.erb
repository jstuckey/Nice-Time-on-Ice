require 'test_helper'

class Links::<%= submodule_name %>::<%= class_name %>Test < ActiveSupport::TestCase

  test "#site_name" do
    skip
    assert_equal "", Links::<%= submodule_name %>::<%= class_name %>.new.site_name
  end

  test "#description" do
    skip
    assert_equal "", Links::<%= submodule_name %>::<%= class_name %>.new.description
  end

  test "#url" do
    skip
    season = seasons(:fourteen)
    team = teams(:caps)
    game_type = "regular"

    url = ""

    link = Links::<%= submodule_name %>::<%= class_name %>.new(team: team, season: season, game_type: game_type)
    assert_equal url, link.url
  end

  test "#group" do
    skip
    assert_equal 0, Links::<%= submodule_name %>::<%= class_name %>.new.group
  end

  test "#position" do
    skip
    assert_equal 0, Links::<%= submodule_name %>::<%= class_name %>.new.position
  end

end
